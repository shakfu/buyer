# Production Environment Variables
# IMPORTANT: Copy this file to .env and update with STRONG passwords and credentials
# DO NOT commit the actual .env file to version control

# PostgreSQL Configuration
POSTGRES_DB=buyer
POSTGRES_USER=buyer
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_REPLICATION_USER=replicator
POSTGRES_REPLICATION_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# MinIO Configuration (Distributed)
# IMPORTANT: MINIO_ROOT_PASSWORD must be at least 32 characters
MINIO_ROOT_USER=CHANGE_ME_ACCESS_KEY
MINIO_ROOT_PASSWORD=CHANGE_ME_SECRET_KEY_MIN_32_CHARS
MINIO_REGION=us-east-1

# Buyer Application Configuration
BUYER_ENV=production
BUYER_USERNAME=CHANGE_ME_ADMIN_USERNAME
BUYER_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# Version (for Docker builds)
VERSION=1.0.0

# Security Notes:
# 1. Use strong, unique passwords for all credentials
# 2. PostgreSQL password should be at least 16 characters
# 3. MinIO secret key must be at least 32 characters
# 4. Change default admin credentials
# 5. Consider using secrets management (e.g., Docker Swarm secrets, Kubernetes secrets)
# 6. Enable TLS/SSL in production
# 7. Restrict network access with firewalls
# 8. Regular security updates and monitoring
