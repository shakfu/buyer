# Buyer Application Environment Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# Environment
# ============================================================================
# Application environment: development, production, or testing
# Default: development
BUYER_ENV=development

# ============================================================================
# Database Configuration
# ============================================================================
# Path to SQLite database file
# Default: ~/.buyer/buyer.db
# Example: /var/lib/buyer/data.db
BUYER_DB_PATH=

# ============================================================================
# Web Server Configuration
# ============================================================================
# Port for the web server to listen on
# Default: 8080
# Example: 3000
BUYER_WEB_PORT=8080

# ============================================================================
# Security Configuration
# ============================================================================
# Enable basic authentication for web interface
# Default: false (no authentication)
# Recommended: true for production
BUYER_ENABLE_AUTH=false

# Basic auth username (REQUIRED if BUYER_ENABLE_AUTH=true, no default)
# Must be set explicitly when authentication is enabled
# BUYER_USERNAME=

# Basic auth password (REQUIRED if BUYER_ENABLE_AUTH=true, no default)
# Must be set explicitly when authentication is enabled
# Password Requirements (enforced when auth is enabled):
#   - At least 12 characters long
#   - Contains at least one uppercase letter
#   - Contains at least one lowercase letter
#   - Contains at least one digit
#   - Contains at least one special character
# Example: SecureP@ssw0rd2024!
# BUYER_PASSWORD=

# Enable CSRF protection
# Default: false
# Recommended: true for production
BUYER_ENABLE_CSRF=false

# Rate limiting is always enabled for security:
#   - General requests: 100 requests per minute per IP
#   - Authentication attempts: 5 attempts per minute per IP (when auth enabled)

# ============================================================================
# Example Configurations
# ============================================================================

# Development (default - no authentication):
# BUYER_ENV=development
# BUYER_WEB_PORT=8080
# BUYER_ENABLE_AUTH=false
# BUYER_ENABLE_CSRF=false

# Production (with authentication - REQUIRED credentials):
# BUYER_ENV=production
# BUYER_DB_PATH=/var/lib/buyer/buyer.db
# BUYER_WEB_PORT=8080
# BUYER_ENABLE_AUTH=true
# BUYER_USERNAME=your-username
# BUYER_PASSWORD=YourSecureP@ssw0rd123!
# BUYER_ENABLE_CSRF=true

# Testing:
# BUYER_ENV=testing
# (Database automatically uses :memory: in testing mode)

# ============================================================================
# How to Use This File
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your configuration
# 3. The application automatically reads from environment variables
#    (You can also set env vars directly without a .env file)
# 4. NEVER commit .env to version control (it's in .gitignore)

# ============================================================================
# Security Notes
# ============================================================================
# - Authentication is DISABLED by default for easier development
# - When enabling auth, you MUST provide username and password (no defaults)
# - Password is hashed using bcrypt before storage/verification
# - CSRF protection should be enabled in production
# - Rate limiting is always active (100 req/min general, 5 req/min auth)
